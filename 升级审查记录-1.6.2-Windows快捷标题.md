# 拾题 Windows 1.6.2 快捷标题升级记录

## 更新内容

- Windows 新建错题窗口新增快捷标题生成器，格式为 `书籍代号-章节号-题号(补充内容)`。
- 预置 `1000a` 至 `1000f`，支持添加或删除自定义书籍；书籍代号可关联可选的真实书名。
- 章节号、题号仅接受非负整数，章节 `0` 合法；补充内容自由填写且允许留空。
- 示例输入会生成 `1000a-0-12(xxxx-xx-xx)`，点击后填入最终标题，仍可继续修改或完全自定义。
- 书籍列表写入 Windows 本地配置并纳入完整备份；删除书籍配置不会改动任何既有错题标题。
- PWA 缓存更新为 v4，确保网页版不会继续使用旧录题页面。

## 兼容与校验

- 标题仍使用原 `title` 字段，题库检索、备份、局域网同步和历史错题无需迁移。
- 共享标题工具已加入 Electron 打包清单，通过纯函数测试、语法检查和浏览器交互验证。
- Windows 版本升级为 1.6.2；Android 保持 1.6.4，Mac 稳定版保持 1.1.0。
- Windows x64 便携版构建成功，版本资源为 `1.6.2`；ASAR 内的 `app.js`、`index.html` 和 `mobile/title-code-utils.js` 与源码哈希一致。
- 桌面程序已更新为 `D:\UserData\asjs\Desktop\拾题考研错题本 1.6.2.exe`；旧 1.6.1 已移入源码工作区的 `_归档\mistake-notebook-windows`，需要时仍可恢复。

# 拾题 1.6.1 Windows 自由裁剪与双路图片升级记录

## 本轮目标

将 1.6.0 已在手机端完成的自由裁剪交互同步到 Windows 桌面端，并补齐题目图片、答案图片分开录入的闭环。保留现有题库、分类树、同步配置和 `E:\错题本数据`，不执行清库或格式重置。

## Windows 端升级内容

- 题目内容改为“文字或题目附件至少一项”，继续兼容 PDF。
- 答案与解析下方新增独立答案图片入口。
- 图片选择后先进入自由裁剪工作台，确认后才写入待保存附件。
- 默认使用小型方形选框；可拖动四角调整大小、拖动中央按钮移动整个选框。
- 图片可用鼠标拖动，使用滚轮、缩放滑杆、加减键或触屏双指缩放。
- 提供自由、原比例、题目横图、方形四种比例。
- 裁剪框和图片均提供方向键微调；按住 Shift 可加大步长。
- 支持重新裁剪、独立移除；取消裁剪不会覆盖此前已选附件。
- 裁剪结果最长边不超过 2048 像素、总像素不超过 400 万，优先压缩为 WebP，失败时回退 JPEG。
- 题目卡展开答案后可显示答案图片；从手机同步来的内嵌题目/答案图片也能在 Windows 端展示。

## 交互与可访问性

- 关键按钮和裁剪控制目标至少 44×44 像素。
- 所有图标式裁剪控制均有可读名称和明显键盘焦点。
- 重要操作不依赖悬停或手势，鼠标、键盘、滑杆均有等效入口。
- 裁剪弹窗支持 Escape 取消，并遵循 `prefers-reduced-motion`。
- 确认裁剪期间按钮进入禁用与“处理中”状态，失败时给出恢复提示。

## 数据兼容

- 原 `attachment` 字段继续表示题目附件。
- 新增 `answerAttachment` 仅表示答案图片；两个附件正文仍写入原 IndexedDB `shiti-assets`。
- 备份继续导出全部附件正文；删除错题时同时清理两路桌面附件。
- 版本升级不会删除 localStorage、IndexedDB、Electron userData 或 `E:\错题本数据`。

## 验证清单

- JavaScript 语法检查：`app.js`、`desktop-cropper.js`、`main.js`，通过。
- 分类、组卷、PDF、裁剪工具、同步服务自动化测试，全部通过。
- 本地浏览器真实交互：题目图片、答案图片、比例切换、方向键移动、确认、重新裁剪、答案展开，通过；控制台无警告和错误。
- PDF 直接附加且不进入裁剪器，通过。
- Windows x64 便携版构建成功，版本资源为 `1.6.1`，包内已核对 `desktop-cropper.js`、`desktop-cropper.css` 和共享 `crop-utils.js`。

## Windows 产物与重装

- 构建产物：`release\拾题考研错题本 1.6.1.exe`
- 桌面程序：`D:\UserData\asjs\Desktop\拾题考研错题本 1.6.1.exe`
- 文件大小：90,657,215 字节（约 86.46 MiB）
- SHA-256：`FA7C17D2963D98E4B2BA26CF492E447032703B928F3279448034320F6133353A`
- 旧桌面 `1.4.0` 已移至：`E:\02-陈禹成文件\代码源码\_归档\mistake-notebook\Windows旧版\拾题考研错题本 1.4.0.exe`
- 新版已成功启动，窗口标题为“拾题 · 考研错题本”，同步端口 `17332` 正常监听，继续使用原 `shiti-kaoyan` 用户数据目录。

## Mac 端说明

本轮没有构建、覆盖或发布 Mac 版。用户已确认 Mac 旧版本曾正常，当前问题按后续升级引入的回归处理；应以最后正常提交为基线做差异审查，不把 Windows 修复直接等同于 Mac 修复。

已从 GitHub Actions 恢复最后一次成功的 macOS 构建：提交 `3689633a56cd21a6db342dfd0b2783ddf42ff154`、运行 `31305774510`、产物 `拾题考研错题本-1.1.0-arm64.dmg`。其 SHA-256 为 `AEBD2DED7A48D15A000B735E03A5596EF5A1C389F05785381C70B113F9EED7B0`，作为后续 Mac 回归修复的稳定基线。

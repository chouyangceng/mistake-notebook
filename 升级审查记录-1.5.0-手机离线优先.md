# 拾题考研错题本 1.5.0 手机端升级审查记录

日期：2026-08-16

## 审查结论

旧版网页虽然用 IndexedDB 暂存未上传题，但 Android 外壳仍强制先连接电脑才能进入页面；上传成功还会删除手机记录，因此不属于真正的手机错题本。本次已改为“本机库是主数据源、电脑同步是可选能力”。

## 第一轮：数据逻辑与功能对齐

- APK 内置手机版 HTML/CSS/JS，未连接电脑也能直接启动。
- 手机独立支持新增、编辑、删除、检索、滚动查看答案及长期保存附件。
- 科目固定为英语、数学、822控制；政治、专业课不进入新配置。
- 知识分类支持多级添加和删除；知识路径与题型是并行字段，并以蓝色/红色分别呈现。
- 英语默认阅读、翻译、作文；数学默认高等数学、线性代数、概率论与四种题型；822 默认七个控制模块与选择题、大题。
- 数学和 822 新增本机智能组卷，按“知识大类 × 题型”自定义数量；英语不进入组卷。
- 复习仅做滚动查看与答案展开，不再设置熟练/不熟练或复习标记。

## 第二轮：同步、迁移与可靠性

- 同步成功不再删除手机题目，只改为“已同步，手机仍保留”。
- 离线新增/编辑记录为待同步；已同步题离线删除生成删除墓碑，下次连接再删除电脑副本。
- 拉取电脑题库时，本机待同步版本不会被远端旧数据或空数据覆盖。
- 服务端支持按 `serverId` 更新已有题目，避免编辑后生成重复题；附件替换或删除会清理旧文件。
- IndexedDB v1 的 `drafts` 自动迁移到 v2 `questions`；由于 Android 1.4.0 使用电脑网址作为存储来源，另提供“找回旧版待上传箱”跨来源迁移入口。
- 本机备份导出包含题库、分类和附件，导入按 ID 与更新时间合并，不清空现有数据。

## 验证结果

- `npm test`：taxonomy、组卷、PDF、同步服务全部通过。
- 新增回归测试：手机同一题二次同步必须更新原题而非重复新增。
- 375 × 812 实机尺寸浏览器检查：无横向溢出；可见按钮、输入框、下拉框均不小于 44px。
- 离线新增后刷新仍存在；编辑后刷新仍存在；按高等数学选择题成功本机组卷。
- Android 1.5.0 debug APK 构建成功，版本号与四个离线资源均已检查。

## 主要文件

- `mobile/index.html`
- `mobile/mobile.js`
- `mobile/mobile.css`
- `mobile/mobile-extra.css`
- `android-app/app/src/main/java/com/shiti/mobile/MainActivity.java`
- `android-app/app/build.gradle`
- `main.js`
- `mobile-server.js`
- `taxonomy-utils.js`
- `tests/sync-server.test.js`

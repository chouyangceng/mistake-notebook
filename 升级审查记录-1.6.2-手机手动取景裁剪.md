# 拾题手机端 1.6.2 手动取景裁剪升级记录

日期：2026-08-21

## 问题结论

相机调用和 Android 文件回传链路本身正常。旧裁剪器的问题在交互层：照片拖动与选框拖动区域重叠，中央“移动选框”按钮遮住主要内容，初始图片又恰好贴合裁剪框边缘，导致手机用户很难确认正在移动什么，并容易误以为不能手动指定裁剪部位。

## 本轮修改

- 拍照或选图后默认进入“移动照片”模式，单指拖动照片即可改变最终保留部位。
- 新增明确的“移动照片 / 移动选框”模式切换；当前模式有高亮、操作说明和实时状态反馈。
- “移动选框”模式允许在画布区域单指拖动整个白框，四角继续负责自由改变范围。
- 删除遮挡内容的中央“移动选框”按钮，改为不拦截触控的底部提示。
- 新增上、下、左、右微调按钮；按钮会按当前模式移动照片或选框。
- 新增显式的缩小、放大按钮，并保留精细缩放滑杆和双指缩放。
- 初始缩放改为最小覆盖比例的 108%，为照片预留可见的移动余量。
- 四角控制点视觉尺寸增大，触控命中区保持不小于 48px。
- 横屏工具区改为独立纵向滚动，避免内容过高时侵入标题栏；消除横向溢出。
- Android 版本升级为 `1.6.2`、`versionCode 10`。

## 使用方法

1. 在题目图片或答案图片区域点击“拍照”。
2. 拍照确认后自动进入裁剪界面。
3. 保持“移动照片”，单指拖照片，让目标内容进入白框。
4. 如需改变白框位置，点击“移动选框”后拖动；如需改变范围，直接拖四角。
5. 可用方向键微调，用减号、加号或滑杆调整照片大小。
6. 点击“确认裁剪”；录入页仍可点击“重新裁剪”。

## 验证结果

- `node --check mobile/mobile.js`：通过。
- `npm test`：分类、组卷、PDF、裁剪几何和同步服务测试全部通过。
- `git diff --check`：通过。
- 375×812 竖屏浏览器回归：界面完整显示，无横向溢出。
- 单指拖照片：状态显示“正在移动照片”，画面位置随手势改变。
- 单指拖选框：选框由 `(94.57, 102.68)` 移动到 `(119.57, 117.68)`。
- 四角拉伸：选框由约 `210×210` 调整为约 `230×230`。
- 方向微调和加减缩放：位置与滑杆数值均按预期变化。
- 812×375 横屏回归：工具区 `clientWidth = scrollWidth = 284`，仅纵向滚动；确认按钮可正常点击。
- 完成裁剪后生成图片预览和“重新裁剪”入口，浏览器控制台无错误。
- Android `assembleDebug`：成功；APK 内四项核心网页资源与源码 SHA-256 完全一致。
- APK 签名：v2 验证通过。
- APK 大小：2,534,821 字节。
- APK SHA-256：`F6ACD4B1E2C0D29DD878B33AB5ACD8371F4E27881AA56FD2C7F477AA444CEA04`。

## 安装包

`D:\UserData\asjs\Desktop\拾题手机端-1.6.2.apk`

当前未检测到已连接的 ADB 设备，因此未直接覆盖安装到手机；可把桌面 APK 发送到手机后安装。若系统提示版本冲突，请先确认旧版数据已导出备份，再决定是否卸载旧版。

# 拾题手机端 1.6.3 图片旋转升级记录

日期：2026-08-22

## 升级内容

- 手机裁剪工作台新增“向左旋转”和“向右旋转”，每次旋转 90°。
- 旋转按钮使用统一的线性 SVG 图标和可见中文名称，不使用表情符号。
- 两个按钮触控高度不小于 48px，并提供明确的按压、键盘焦点和无障碍标签。
- 旋转后自动重新计算图片宽高、最小缩放比例和裁剪范围，仍可继续拖动照片、移动选框、拉伸四角及缩放。
- “原比例”会根据当前旋转方向自动交换宽高比。
- 最终 WebP/JPEG 裁剪输出会保持当前旋转方向，不会只旋转预览。
- 旋转采用画布坐标变换，不额外生成一张完整像素副本，降低高像素手机照片的峰值内存占用。
- Android 版本升级为 `1.6.3`、`versionCode 11`。

## 验证结果

- `node --check mobile/mobile.js`：通过。
- `npm test`：分类、组卷、PDF、裁剪几何和同步服务测试全部通过。
- 新增旋转圈数归一化、90°/270°宽高交换测试。
- 375×812 竖屏：左右旋转按钮完整显示，无横向溢出。
- 向右旋转：预览方向与“已向右旋转 90°”状态一致。
- 向左旋转：预览方向与“已向左旋转 90°”状态一致。
- 旋转后确认裁剪：生成的 WebP 预览保持旋转方向，并保留“重新裁剪”入口。
- 812×375 横屏：工具区 `clientWidth = scrollWidth = 284`，仅纵向滚动，无横向溢出。
- 浏览器控制台无错误。
- Android `assembleDebug`：成功；版本 `1.6.3`、`versionCode 11`、targetSdk 35。
- APK 内 `index.html`、`mobile.js`、`mobile-extra.css`、`crop-utils.js` 与源码 SHA-256 完全一致。
- APK v2 签名验证通过。
- 验证构建大小：2,535,721 字节。
- 验证构建 SHA-256：`F549FB568E3DFF2C4DB0975D49C08683069B352263073621EDA4276D6A1CAB69`。

## 发布方式

按当前交付要求，本轮只提交源码和记录到 GitHub，不复制 APK 到桌面，也不把调试 APK 纳入 Git 仓库。

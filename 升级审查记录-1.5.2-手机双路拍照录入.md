# 拾题 1.5.2 手机双路拍照录入升级记录

日期：2026-08-17

## 本轮升级

- 将题目附件入口移动到题目内容下方，拆分为“拍照”和“相册 / PDF”。
- 在答案 / 解析下方新增独立的“拍照”和“从相册选择”。
- 题目图和答案图分别保存状态，互不覆盖，可各自预览、重新裁剪和移除。
- Android 拍照确认后直接打开内置裁剪器；相机临时文件通过 `FileProvider` 安全回传。
- 两路图片裁剪后压缩为 WebP，随错题写入手机 IndexedDB，重启和离线状态下仍保留。
- 题库答案面板与手机组卷答案详情支持显示答案图片。
- 更换 Android 自适应图标：深森林绿底、象牙白错题卡、珊瑚色叠卡和问号标记，并提供 Android 13 单色图标。

## 权限与兼容性

- 未增加 `CAMERA`、相册或外部存储权限，拍摄由系统相机应用完成。
- 相机只获得单次临时文件 URI 的读写权限。
- 彩色自适应图标兼容 Android 8.0+，单色主题图标限定 Android 13+。
- 题目 PDF 仍可直接录入且不进入图片裁剪；答案区域只接受图片。

## 验收范围

- JavaScript 语法检查、仓库自动化测试和补丁格式检查。
- Android Gradle 编译、Manifest/资源打包及 APK 版本检查。
- 手机竖屏与横屏布局、题目/答案独立裁剪、保存后刷新、重新裁剪和独立删除。

## 验收结果

- `node --check mobile/mobile.js`：通过。
- `npm test`：5 组测试全部通过。
- Android `assembleDebug`：通过，`versionCode=8`、`versionName=1.5.2`。
- APK 签名校验：Android v2 签名有效。
- 竖屏 `375×812`：题目图与答案图可分别裁剪、预览、保存，刷新后均保留，无横向溢出。
- 横屏 `812×375`：裁剪画布、比例、缩放、重置和确认按钮完整可见，无横向溢出。
- PDF 边界：题目 PDF 跳过裁剪并保留；答案区域拒绝 PDF。
- 桌面 APK：`D:\UserData\asjs\Desktop\拾题手机端-1.5.2.apk`，2,528,121 字节。
- SHA-256：`550027F8D1AABEE009E2AD76F99A8D16BCDA941DE30D0B7E24DF5551B2CAACBF`。

## 数据说明

答案图片当前以手机本地数据为准，完整包含在手机备份中。与电脑同步时不会因远端回拉而覆盖手机已有答案图片；Windows 端展示答案图片属于后续跨端扩展范围。

# 拾题项目审查报告

审查日期：2026-08-09

## 结论

当前版本适合作为“单机原型 + 局域网实验版”，还不适合直接作为跨设备的可靠主库。核心录题、答案分步展示、复习次数记录和按模块排题已经具备，但同步、附件处理、删除/编辑闭环和发布流程仍有明显缺口。

## 问题清单

### P0：同步可能造成数据覆盖

`app.js` 的同步流程先把当前设备的整包数据 `POST` 到 Windows，再处理服务端返回值；`main.js` 的 `mergeState()` 对 `plan`、`done`、`dayPlan` 等结构是整对象替换。两个设备同时修改时，后提交的一方可能覆盖另一方的计划或完成记录。题目本身按 `updatedAt` 做了较粗的合并，但删除标记、附件和配置没有完整的冲突策略。

建议：同步接口改成“拉取版本/提交变更”或至少按实体分别合并；为计划、完成记录、配置和附件增加 `updatedAt` / 版本号；冲突时保留双方数据并提示用户。

### P0：局域网同步无鉴权

Windows 服务监听 `0.0.0.0:17332`，`GET` 可以读取完整题库，`POST` 可以写入完整题库，当前没有口令、配对码、来源校验或 HTTPS。连接同一 Wi‑Fi 的其他设备可以直接读写数据。

建议：首次启动生成随机配对码，所有 GET/POST 要求 `Authorization`；至少提供“仅本机 / 局域网”开关，并在文档里明确局域网风险。

### P1：PDF 功能与需求不一致

目前 PDF 仅保存附件名/正文数据，界面不展示 PDF 内容，也没有页码选择、截图裁剪、题目图与答案图分离或 OCR。用户提出的“第一次只展示题目、第二次点击展示答案”只对文字答案生效，PDF 内的答案不能分区管理。

建议：先实现最小闭环：PDF 预览 + 页码/区域裁剪 + `questionAssetId` / `answerAssetId` 两个附件字段；OCR 放到后续版本。

### P1：编辑、删除、归档未形成闭环

数据模型已有 `deletedAt`，但界面没有编辑、删除、恢复和附件清理入口。用户录错后只能导出/导入整库修正，长期使用会积累无主附件。

建议：增加编辑和软删除；删除题目时同步清理未被其他题引用的附件；提供“已归档”筛选和恢复。

### P1：计划状态可能被跨端快照覆盖

`dayPlan` 是当天题单快照，跨设备同步时直接替换；一台设备完成题目后，另一台设备可能把旧快照推回，导致今日进度回退或重复。`done` 也是整对象替换。

建议：完成记录按日期做集合并集；当天题单按题目 ID 合并，或在每次同步后重新计算题单。

### P1：日历和日期展示是硬编码

`calendar()` 固定使用 `new Date(2026, 7, 1)`，页面文案也固定显示“2026年8月”，与当前日期无关。复习数据真实按系统日期记录，因此日历展示会逐月失真。

建议：使用 `today()` 生成当前年月，并根据当月天数、首日星期和真实完成记录渲染。

### P2：存储容量和性能风险

图片/PDF 以 Base64 放入 IndexedDB，并在同步时把全部附件放进一个 JSON 请求；30 MB 只是服务端请求上限，移动端浏览器和 localStorage/IndexedDB 仍可能因大文件失败。每次 `render()` 还会重新读取并重绘全部卡片。

建议：限制单附件大小；附件单独分片/按 ID 增量同步；题库较大时采用分页或虚拟列表。

### P2：输入与兼容性问题

题目 ID 使用 `Date.now()`，极短时间连续录入可能碰撞；跨端导入后也可能出现相同 ID。同步地址直接由用户填写，错误地址和非 HTTPS 地址没有明确校验。`navigator.clipboard` 在部分浏览器/非安全上下文不可用，当前只能提示失败。

建议：统一使用 UUID；限制 endpoint 为 `http(s)`；复制失败时提供选中文本或手动复制提示。

### P2：发布链路尚未闭合

`package.json` 只有 `electron-builder --win portable`，没有锁定依赖的 lockfile，也没有 macOS/Android 构建配置、自动更新、签名或版本迁移策略。当前目录中的 `.command` 适合 macOS，但 Windows 用户仍需要自己安装 Node/npm 才能构建。

建议：先补齐 Windows 打包验证和安装说明，再决定 Android 是 PWA 还是 Capacitor/TWA；不要在同步协议稳定前急于多端原生化。

## 建议迭代顺序

1. 先修 P0：鉴权、备份恢复、冲突/合并策略。
2. 再修 P1：编辑删除、真实日历、题目/答案附件分离。
3. 最后做 P2：大附件增量同步、构建矩阵、移动端封装。

## 验证限制

本次静态审查环境中未找到 `node` / `npm` 可执行文件，因此无法在当前 shell 重新执行语法检查或 Electron 启动测试；需要在安装 Node.js 的环境补跑 `node --check app.js`、`node --check main.js` 和 `npm run start`。

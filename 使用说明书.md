# 拾题 · 考研错题本 1.5.1 使用说明书

## 1. 使用入口

- Windows：运行便携版 EXE，或在源码目录执行 `npm start`。
- 浏览器/PWA：通过本地网页服务或在线地址打开；数据属于当前浏览器配置。
- Android：安装 APK 后可直接离线使用，连接 Windows 只是可选同步能力。

## 2. 录入错题

1. 选择英语、数学或822控制。
2. 选择独立题型。
3. 沿蓝色知识分类逐级选择；可在「科目配置」继续添加任意层级。
4. 填写具体知识点、题干、答案；标签、反思和结论可选。
5. 图片或 PDF 可作为附件保存；手机选择图片后会先进入裁剪界面。

科目默认结构：

- 英语：阅读、翻译、作文。
- 数学：高等数学、线性代数、概率论；题型为选择题、填空题、大题、证明题。
- 822控制：按控制理论知识大类；题型为选择题、大题。

## 3. 分类维护

- 蓝色标签代表知识分类，点击「加下级」可继续分级。
- 红色标签代表题型，与知识路径并行，不属于知识分类的下一级。
- 分类和题型均可添加、删除；删除配置不会擅自删除已有错题，题目保留原分类文字，之后可手动重新归类。
- 每张题卡的「修改归类」只修改科目、知识路径、题型、知识点和标签，不改题干与答案。

## 4. 滚动复习

进入「滚动复习」后从上到下浏览全部错题，需要时展开答案即可。

本版本不记录熟练度，不设置“不会/模糊/掌握”，不自动安排1/3/7天复习，也不要求完成标记。

## 5. 智能组卷

智能组卷仅用于数学和822控制。

1. 在“知识大类 × 题型”矩阵里逐项填写题数。
2. 输入试卷名称并生成。
3. Windows 桌面版会生成试卷版和答案版 PDF，并保存组卷历史。
4. 手机端在本机题库内抽题展示；题量不足时会提示缺口。

## 6. Android 离线使用

- 未填写电脑地址和配对令牌时，录入、编辑、删除、检索、分类和组卷仍可正常使用。
- 保存成功即写入手机 IndexedDB，退出应用或重启手机后仍保留。
- 图片裁剪支持拖动、双指或滑杆缩放，以及原比例、题目横图、方形三种比例；确认后才会替换当前附件。
- 裁剪结果优先保存为 WebP，最长边不超过 2048px、总像素不超过 400 万；PDF 不进入裁剪界面。
- 已保存的本机图片可在编辑题目时点击「重新裁剪」，取消裁剪不会覆盖原附件。
- 同步成功后题目仍留在手机，只更新同步状态。
- 「本机备份」可导出/导入题目和附件；旧版用户可使用「找回旧版待上传箱」。

## 7. 局域网同步

Windows 默认同步地址：

```text
http://Windows电脑的局域网IP:17332
```

1. 让手机和 Windows 连接同一可信局域网。
2. 打开 Windows 桌面版，在使用指引查看地址与配对令牌。
3. 在手机「分类与同步」填写两项，先检查连接，再同步本机变更。

所有请求都必须携带随机配对令牌。局域网接口是 HTTP，不要暴露到互联网，也不要向无关人员分享令牌。

## 8. 删除规则

- 删除前必须二次确认。
- 已与 Windows 建立同步的题目会写入删除墓碑，避免旧设备下次同步时把题目复活。
- 手机离线删除会先保存在本机，下次连接时再同步删除状态。
- 如果删除后界面仍显示，先确认运行的是 1.5.1；新版 PWA 使用网络优先缓存，刷新即可取得最新逻辑。

## 9. 备份建议与限制

- 大改分类、换机或批量录入前先导出备份。
- 附件会明显增大同步包；超大 PDF 建议拆分后再录入。
- 当前没有公网账号和云同步；跨设备依赖备份文件或可信局域网同步。
- 同一题在多设备修改时按 ID、更新时间和版本合并，时间较新的版本优先。

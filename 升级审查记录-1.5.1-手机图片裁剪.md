# 拾题考研错题本 1.5.1 手机图片裁剪升级记录

日期：2026-08-17

## 升级结论

手机端图片附件已从“选中后直接保存”升级为“选图、裁剪、压缩、确认、保存”的完整流程。PDF 继续沿用原附件流程；裁剪结果仍使用原有 `file` 数据结构写入 IndexedDB，因此离线保存、编辑、备份和局域网同步协议不需要迁移。

## 第一轮：主流程实现

- 选择 JPG、PNG、GIF 或 WebP 后自动进入全屏裁剪层，取消不会替换原附件。
- 支持单指拖动、双指缩放、可见缩放滑杆、鼠标滚轮和键盘方向键。
- 提供原比例、题目横图（4:3）、方形三种裁剪比例及一键重置。
- 确认后优先输出质量 0.86 的 WebP；环境不支持 WebP 时回退 JPEG。
- 输出最长边限制为 2048px、总像素限制为 400 万，避免 IndexedDB、备份和同步包异常膨胀。
- 附件预览新增「重新裁剪」；编辑旧题时可继续裁剪已有本机图片。
- PDF 不进入裁剪器，继续直接保存为本机附件。

## 第二轮：边界与可用性审查

- 裁剪层适配 375×812 竖屏和 812×375 横屏，横屏使用画布与工具栏并排布局，无横向溢出。
- 取消、重置、比例和确认控件均不小于 44px，并保留明确的按压与焦点状态。
- 比例按钮通过 `aria-pressed` 报告选中状态；弹层具有对话框语义、焦点循环和 Escape 退出。
- 确认裁剪后焦点回到「重新裁剪」，处理期间确认按钮禁用，失败时在裁剪层内提供恢复提示。
- 无效图片读取失败后会清空文件控件，允许立即重新选择同一文件。
- 补充 `image/jpg` 相机兼容类型；GIF 裁剪输出为静态 WebP/JPEG。
- Android 构建脚本不再依赖源码内的中文目标路径，避免 Windows PowerShell 旧编码导致最后复制失败。

## 验证结果

- `node --check mobile/mobile.js`：通过。
- `npm test`：分类、组卷、PDF、裁剪几何、同步服务全部通过。
- 新增裁剪几何测试：视口比例、最小缩放、边界夹取、焦点缩放、2048px/400万像素输出限制。
- 浏览器 375×812：选图、比例切换、缩放、WebP 输出、预览和重新裁剪通过，控制台无错误或警告。
- IndexedDB：保存裁剪图后刷新页面，题目和 WebP 图片仍在本机题库。
- 编辑流程：旧图可重新裁剪；取消后原附件仍保留。
- 浏览器 812×375：画布 367×276、工具栏 300px，无横向溢出。
- Android `assembleDebug`：成功；版本 `1.5.1`、`versionCode 7`、targetSdk 35。
- APK 大小：2,240,269 字节；包内存在 `index.html`、`mobile.js`、两份 CSS 和 `crop-utils.js`。
- APK SHA-256：`35C8922653E3B97544CFD58F55CF9F10B27CCB4683FBB20DFB312289C7B16093`。

## 主要文件

- `mobile/index.html`
- `mobile/mobile.js`
- `mobile/mobile.css`
- `mobile/mobile-extra.css`
- `mobile/crop-utils.js`
- `tests/crop-utils.test.js`
- `android-app/app/build.gradle`
- `android-app/构建安卓APK.ps1`

## 安装包

`D:\UserData\asjs\Desktop\拾题手机端-1.5.1.apk`

构建时未检测到 ADB 设备，因此没有直接覆盖安装到手机；安装包本身已完成版本、资源、权限和哈希核验。

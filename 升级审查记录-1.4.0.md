# 拾题考研错题本 1.4.0 升级审查记录

## 本轮目标

- 固定科目为英语、数学、822控制，政治与专业课不再进入正常导航和录题流程。
- 将旧“模块/单元”升级为可无限向下扩展、可删除的知识分类树。
- 新增与知识路径并行的独立题型维度：数学为选择题、填空题、大题、证明题；822控制为大题、选择题。
- 英语知识大类调整为阅读、翻译、作文、小三门，不参与智能组卷。
- 智能组卷改为数学/822控制的“知识大类 × 题型”数量矩阵。
- 复习改成纯滚动浏览，不再记录不会、模糊、掌握或安排复习日期。

## 数据兼容

- 保留旧题和旧复习历史，不执行题库清空。
- 旧 `module`、`unit` 自动迁移为 `knowledgePath`，并继续写入兼容字段。
- 无法可靠判断的旧题型标为“未分类题型”，不做主观猜测。
- 政治和专业课旧题仍在备份/同步数据中，但不显示在正常操作界面。

## 两轮审查与验证

### 第一轮：结构与逻辑

- 新增独立的 `taxonomy-utils.js`，负责默认配置、旧配置迁移、路径规范化、节点新增和删除。
- 录题、修改归类、科目配置、智能组卷、PDF 元信息和手机上传链路统一采用新分类模型。
- 手机网页端与 Android APK 同步升级：连接 Windows 后读取最新知识树，支持逐级知识分类与独立题型，离线草稿使用同结构默认配置。
- 删除操作均提供明确确认；知识分类用蓝色文字标签，题型用红色文字标签，不只依赖颜色。

### 第二轮：兼容与可用性

- 检查同步、备份导入和旧 localStorage 配置，统一经过白名单与迁移函数。
- 增加分类迁移、任意层级增删、知识大类与题型联合组卷、缺题提示测试。
- 执行 JavaScript 语法检查，分类、组卷、PDF 附件和同步服务全部自动化测试通过。
- 在本地真实页面逐项核验三科导航、数学题型、知识树配置、组卷矩阵与滚动复习，控制台无错误。
- 在 375×812 手机视口核验 Windows 分类同步及三级知识路径（高数 → 极限 → 数列极限），草稿正确保存完整路径与题型，控制台无错误。

## 版本与发布

- 源码版本从 1.3.0 升级为 1.4.0。
- 已生成 `release\拾题考研错题本 1.4.0.exe`，桌面 1.3.0 保留为回退版本。
- 已构建 Android `1.4.0` 调试安装包，并复制到桌面供直接安装。
- 移除顶部无实际功能的搜索按钮和“林”头像入口，仅保留核心的“新建错题”操作。

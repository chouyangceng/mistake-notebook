# 拾题 1.6.0 文件夹分类与自由裁剪升级记录

日期：2026-08-20

## 分类逻辑升级

- 知识分类树按文件夹逻辑管理，桌面端与手机端均支持添加根分类、添加下级、重命名、移动和删除。
- 移动分类时不能选择自身或自身下级，不能在目标位置产生同名分类。
- 分类重命名或移动后，旗下所有旧错题的完整知识路径自动同步变化。
- 删除分类不会删除错题：关联旧错题自动移到上一级；删除根分类时移入“未分类”。
- 手机题库卡片新增“移动分类”，以前录入的错题可直接选择后来新增的任意层级；“编辑全部”仍保留完整编辑能力。
- 桌面错题卡原“修改归类”升级为“移动 / 修改归类”，同样支持新层级。

## 图片裁剪升级

- 固定裁剪画布改为独立的自由裁剪选框，默认显示较小方框。
- 四个角各提供 48px 触控区域，可自由改变选框宽度和高度。
- 选框中部提供“移动选框”，可整体移动裁剪区域。
- 图片区域支持单指拖动与双指捏合缩放；两指背向移动时实时放大图片。
- 保留缩放滑杆作为非手势替代，并保留自由、原比例、题目横图、方形四种模式。
- 图片始终覆盖裁剪选框，移动或放大选框后会自动修正图片边界，避免裁出透明/空白区域。
- 最终 WebP/JPEG 输出只取选框覆盖的原图区域。

## UI / 可用性原则

- 文件夹移动使用明确的目标路径下拉框和确认按钮，不要求用户记忆路径或只依赖拖拽手势。
- 所有新增触控目标按至少 44–48px 设计；操作同时提供文字和无障碍名称。
- 双指缩放不是唯一入口，滑杆、键盘和鼠标滚轮操作继续保留。
- 移动、重命名和删除分类后均给出旧错题迁移数量反馈。

## 验收结果

- `npm test`：taxonomy、exam、PDF、crop、sync 5 组测试全部通过。
- `node --check app.js` 与 `node --check mobile/mobile.js`：通过。
- 手机竖屏 `375×812`：自由选框四角拖动、整框移动、缩放滑杆、裁剪输出和重新裁剪通过，无横向溢出。
- 手机横屏 `812×375`：四个比例按钮、缩放、重置和确认操作完整可见，无横向溢出。
- 分类真实链路：旧错题从“高等数学 › 浏览器迁移测试”随分类移动到“线性代数 › 浏览器迁移测试”。
- 桌面分类页：新增下级、重命名、移动、删除入口和目标路径面板正常，无控制台错误。
- Android `assembleDebug`：通过，`versionCode=9`、`versionName=1.6.0`。
- APK v2 签名校验通过；APK 内包含新版 `index.html`、`mobile.js` 与 `crop-utils.js`。

## Android 产物

- 路径：`D:\UserData\asjs\Desktop\拾题手机端-1.6.0.apk`
- 大小：2,555,613 字节（约 2.44 MB）
- SHA-256：`6BAB0C854FBD7441E847EAD2DFB9D421A20B52648DAAF3647D2F1F24DE638D66`
- 当前 ADB 未检测到连接设备，因此已生成安装包但未自动安装到手机。

## 同步说明

分类配置与错题新路径都会被标记为本机变更。连接 Windows 后执行同步，即可把分类树和旧错题迁移后的路径一起写回电脑题库。

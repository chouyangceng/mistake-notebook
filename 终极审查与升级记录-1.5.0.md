# 拾题错题本 1.5.0 终极审查与升级记录

审查日期：2026-08-16

## 结论

本轮按“数据不复活、离线不丢题、旧逻辑不潜伏、安装包能真实构建”的标准完成两轮审查。桌面端、浏览器端和 Android 端已统一到英语、数学、822控制三科，多级知识分类与独立题型并行；滚动复习不再包含熟练度和间隔复习逻辑。

## 第一轮：架构、数据与安全

- 将 Electron 从 31 升级到 43.4.0，electron-builder 从 24 升级到 26.15.3；官方 npm 安全审计从 1 个 critical、10 个 high 降为 0。
- 清除 `app.js` 中 15 组重复函数和整套已废弃的熟练度复习、四科计划、旧分类、旧组卷实现，消除“后声明覆盖前声明”的幽灵逻辑。
- 分类配置增加版本迁移：旧英语“小三门”会移除，旧空题型补齐阅读/翻译/作文；迁移后尊重用户后续主动增删。
- 新安装不再自动写入三道演示题，避免演示数据污染真实题库。
- Electron 启用 sandbox、关闭 Node 集成、保持上下文隔离和 Web 安全，限制新窗口与页面跳转，并拒绝运行时权限请求。
- 桌面与手机页面增加 CSP，限制脚本、对象、表单和资源来源。
- Android 改用 AndroidX `WebViewAssetLoader`，删除 `file://` 万能跨域访问、旧隐藏连接页、UDP 发现线程和无用 Wi-Fi 权限；只保留联网权限、附件选择及旧配置一次性迁移。
- 新增 `.gitignore`，排除依赖、构建目录、APK/EXE、Android SDK 本机路径、测试数据、日志和代理工作笔记。

## 第二轮：交互、缓存与发布

- 用真实浏览器验证桌面新增错题、数学证明题分类、刷新持久化、跨科目重新归类和多级子分类添加。
- 用真实浏览器验证 Android 页面在未连接电脑时写入 IndexedDB，刷新后题目、知识路径和题型仍存在。
- 验证 375×812 竖屏和 812×375 横屏无水平溢出、无控制台错误。
- 系统 `prompt()` 替换为桌面/手机正式输入弹窗，分类新增不再依赖容器是否支持阻塞弹窗。
- PWA 缓存升级为 v3 网络优先；补齐 `taxonomy-utils.js` 和附加样式的离线缓存，避免升级后第一次打开仍运行旧删除逻辑。
- 将旧“复习进度/连续学习/复习日历”改为近7天入库、知识分类数量、错题焦点和入库日历，避免无熟练度模式下展示永远为零的假指标。
- README、Android 说明和使用说明书已按 1.5.0 的真实逻辑重写。

## 验证证据

- 所有 JavaScript 文件通过 `node --check`。
- `npm test`：分类迁移、组卷选择、PDF 附件、同步服务全部通过。
- `npm audit --registry=https://registry.npmjs.org`：0 vulnerabilities。
- Android `assembleDebug`：成功；APK 版本 1.5.0，targetSdk 35，2.13 MB，包内含完整手机版资源，仅声明 INTERNET 及 AndroidX 自动生成的内部非导出权限。
- Windows `electron-builder --win portable`：成功；便携版 1.5.0 为 86.44 MB，未纳入源码仓库。
- Windows 解包版使用隔离 userData 完成主进程与渲染进程启动冒烟测试。

## 产物

- Android：`D:\UserData\asjs\Desktop\拾题手机端-1.5.0.apk`
- Windows：`release\拾题考研错题本 1.5.0.exe`（构建产物，仅本地保留）

## 仍需明确的边界

- 局域网同步仍是 HTTP，只适合可信个人网络，不应直接暴露公网。
- Android 当前为 debug 签名包；正式商店发布需要用户自己的 release keystore。
- 本机只验证了 Windows 和 Android；macOS/Linux 交由 GitHub Actions 构建矩阵验证。
- Windows 便携版包含 Chromium，体积明显大于 APK；GitHub 源码提交不会包含该 EXE。
